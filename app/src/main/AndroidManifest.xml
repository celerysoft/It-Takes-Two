<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.celerysoft.ittakestwo" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.LaunchingActivity"
            android:label="@string/title_activity_playing_card">

        </activity>
        <activity
            android:name=".activities.PlayingCardActivity"
            android:label="@string/title_activity_playing_card"
            android:theme="@style/PlayingCardActivityTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
